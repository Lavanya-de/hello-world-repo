name: Hello World

on:
  push:

jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo text
        run: echo "Hello, world!"

  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo test text
        run: echo "test the Hello, world!"